package spill

import (
    "testing"
)

// No tests for envelope file writes in current scope.
func TestNoop(t *testing.T) {
    t.Skip("spill writer envelope tests are not applicable in this PR scope")
}
